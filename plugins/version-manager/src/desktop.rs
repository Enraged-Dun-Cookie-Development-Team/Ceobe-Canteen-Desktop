use serde::de::DeserializeOwned;
use tauri::{plugin::PluginApi, AppHandle, Runtime};

use crate::models::*;

pub fn init<R: Runtime, C: DeserializeOwned>(
  app: &AppHandle<R>,
  _api: PluginApi<R, C>,
) -> crate::Result<VersionManager<R>> {
  Ok(VersionManager(app.clone()))
}

/// Access to the version-manager APIs.
pub struct VersionManager<R: Runtime>(AppHandle<R>);

impl<R: Runtime> VersionManager<R> {
  pub fn ping(&self, payload: PingRequest) -> crate::Result<PingResponse> {
    Ok(PingResponse {
      value: payload.value,
    })
  }
}
